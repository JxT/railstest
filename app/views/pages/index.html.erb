<h1>Best girl site</h1>

<% if !current_user %>
	<p>Please <%= link_to "login", new_user_session_path %> and rank your best girl!</p>

<% end %>

<p> Best girls are: </p>

<table>
	<tr>
		<th>Name</th>
		<th>Total Score</th>
		<th>Position</th>
	</tr>

	<% (0..Girl.all.count - 1).each do |i| %>
		<tr>
			<td><%= Girl.find_by(id: @globalRanking[i][0]).name %></td>
			<td><%= @globalRanking[i][1] %></td>
			<td><%= (i + 1).ordinalize %></td>
		</tr>
	<% end %>
</table>